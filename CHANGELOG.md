# Changelog

## 1.3.1 - 2026-08-19

- 移除天气块在地址标签为空时回退输出经纬度坐标的逻辑，天气和位置块均不再注入坐标。
- 修复自动定位缓存未包含实际位置的问题：自动模式每次先获取当前坐标，再按定位配置和坐标键复用反向地址缓存。
- 用户移动到不同坐标区域后会自动切换地址缓存键，并由天气缓存按新坐标独立查询或复用。
- 保留手动地址缓存行为；定位刷新间隔在自动模式下用于控制同一坐标区域的反向地址解析缓存。
- 增加移动前后地址切换、同位置地址缓存命中和编译产物无坐标回退的回归测试。

## 1.3.0 - 2026-08-19

- 天气供应商新增 Auto 模式。
- Auto 按 Open-Meteo → MET Norway → wttr.in 顺序尝试，首个成功结果即被采用。
- Auto 容错结果记录前序失败原因和最终使用的天气源，并按 Auto 独立缓存。
- 保留原有单供应商模式及 MET Norway、wttr.in 失败回退 Open-Meteo 的兼容行为。
- 增加 Auto UI、供应商调用顺序、逐级容错和缓存命中自动测试。

## 1.2.0 - 2026-08-19

- 新增天气和位置/地址缓存，普通预览及聊天注入在刷新间隔内复用缓存。
- 天气缓存按供应商和坐标隔离；位置缓存按定位模式、手动地址、精度模式和地址解析供应商隔离。
- 新增天气刷新间隔（5–180 分钟，默认 30）与定位刷新间隔（5–60 分钟，默认 10）。
- 设置页在“预览与测试”前新增“刷新间隔”设置节和独立“保存设置”按钮。
- “手动测试”会绕过位置与天气缓存并强制刷新；“更新注入预览”继续使用缓存。
- 每类缓存最多保留 12 个最近配置键，避免切换配置时无限增长。

## 1.1.2 - 2026-08-19

- 注入文本块顺序调整为：当前时间、当前天气、当前位置、当前电量、设备信息。
- 当前位置不再注入经纬度坐标和定位时间。
- 成功与失败降级块均保持同一固定顺序。

## 1.1.1 - 2026-08-19

- 在自定义设备名称输入框下增加“保存设置”按钮。
- 在手动地址输入框下增加“保存设置”按钮。
- 超时、设备名称和手动地址保存按钮统一命名为“保存设置”。
- 移除要求点击旧按钮名称的过时说明。

## 1.1.0 - 2026-08-19

- 新增自定义设备名称，设置后优先于Android系统设备名称。
- 参考官方世界书插件新增角色卡绑定，并支持多选。
- 未选择角色卡时保持全局注入；选择后仅匹配当前活动角色卡ID。
- 角色卡列表通过官方`Tools.Chat.listCharacterCards()`读取，Hook优先使用`metadata.activePrompt`并以`getCallerCardId()`兜底。
- 增加设备名称覆盖、角色卡匹配/拦截、列表和UI渲染自动测试。

## 1.0.0 - 2026-08-18

- 首次发布 Operit 环境信息注入 ToolPkg。
- 支持时间、天气、地点、电量和设备信息五类注入。
- 支持注入内容随消息保存或仅在发送给模型前临时注入。
- 支持 3–60 秒注入总超时，默认 10 秒。
- 支持设备自动定位和手动地址。
- 支持 Auto、Nominatim、BigDataCloud、Photon 反向地址解析。
- 支持 Open-Meteo、MET Norway、wttr.in 天气供应商及有界失败回退。
- 提供 Compose DSL 设置页、条件显隐、内容预览和手动测试。
- 完成编译、模拟宿主、包结构、烧录及真机消息注入验证。
